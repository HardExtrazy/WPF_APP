<Window x:Class="WPF_APP.RegistrationForm"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_APP"
        mc:Ignorable="d"
        Title="RegistrationForm" Height="650" Width="500" MinHeight="450" MinWidth="600"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent" 
        ResizeMode="NoResize">
    <Border CornerRadius="20" BorderThickness="1" Background="#b4b6b5"  Margin="10">
        <Grid>
            <Border CornerRadius="20" BorderThickness="1" Background="#eaf0f3" Margin="10">
                <StackPanel Width="550" Height="600">
                    <StackPanel HorizontalAlignment="Right" Margin="10 5">
                        <Button x:Name ="Exit" Style="{StaticResource MaterialDesignFloatingActionButton}" Background="#1d7d97" Foreground="#16596b"
                            ToolTip="Закрыть" Click="btnExit_Click" BorderBrush="#16596b" Cursor="Hand" Width="25" Height="25">
                            <materialDesign:PackIcon Kind="Close" Foreground="#eaf0f3" Width="15" Height="15"/>
                        </Button>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Создайте свой аккаунт" Foreground="Black" TextAlignment="Center" FontSize="28" FontFamily="Segoe Print" Margin="10"/>
                    </StackPanel>
                    <StackPanel Height="500" Width="270" HorizontalAlignment="Center" Margin="0 0 280 0">

                        <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5" 
                          Width="200" Margin="5" Cursor="">
                            <StackPanel Orientation="Horizontal" Cursor="">
                                <materialDesign:PackIcon Kind="User" Width="30" Height="30" Margin="5 0" VerticalAlignment="Center" Cursor=""/>
                                <TextBox x:Name="txtUsername" Width="155" materialDesign:HintAssist.Hint="Введите имя пользователя"
                                 materialDesign:HintAssist.Foreground="Black" FontSize="12" 
                                 materialDesign:TextFieldAssist.UnderlineBrush="Transparent" Foreground="Black" BorderBrush="Transparent" 
                                 CaretBrush="Black" SelectionBrush="Black" Cursor="Pen" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                            </StackPanel>
                        </Border>

                    </StackPanel>
                    <StackPanel Height="500" Width="270" HorizontalAlignment="Center" Margin="0 -500 -280 0">
                        
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
